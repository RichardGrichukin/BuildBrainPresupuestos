<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Generar Presupuesto</title>
    <!-- Incluimos Bootstrap y FontAwesome -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
    <link rel="stylesheet" href="{{ url_for('static', filename='styles.css') }}">
    <style>
        /* Ajustes de estilo para el fondo y el cuadro de carga de imagen */
        body {
            background-color: #34495e; /* Fondo principal gris oscuro */
        }

        .container {
            background-color: #34495e; /* Fondo del contenedor en gris oscuro */
            color: white; /* Texto en blanco */
            padding: 30px;
            border-radius: 8px;
        }

        .card {
            background-color: #2c3e50; /* Color de fondo del cuadro */
            color: white; /* Texto en blanco */
            border: none;
        }

        .main-content h1 {
            color: white; /* Título en color blanco */
            background-color: #2c3e50; /* Fondo del título */
            padding: 20px;
            border-radius: 8px; /* Bordes redondeados */
            text-align: center;
        }

        .generate-btn {
            background-color: #4CAF50; /* Verde para el botón */
            color: white;
        }

        .logo {
            font-size: 36px;
            color: #2c3e50; /* Color original para "BuildBrain" */
        }

        .logo span {
            color: #4CAF50; /* Verde para "Presupuesto" */
        }

        .footer {
            background-color: #2c3e50; /* Fondo del pie de página */
            color: white;
        }
    </style>
</head>
<body>
    <!-- Cabecera con el título personalizado -->
    <nav class="navbar navbar-dark bg-dark mb-4">
        <span class="navbar-brand mx-auto logo"><b>BuildBrain<span>Presupuesto</span></b></span>
    </nav>

    <!-- Contenedor principal con fondo gris oscuro -->
    <div class="container">
        <div class="main-content">
            <h1>Cargar Imagen de Plano</h1>
            <div class="card p-4 mt-3 shadow-sm mx-auto" style="max-width: 600px;">
                <!-- Formulario para subir la imagen -->
                <form action="{{ url_for('upload') }}" method="post" enctype="multipart/form-data" class="mb-3">
                    <div class="form-group">
                        <label for="file" class="font-weight-bold">Seleccionar archivo de imagen:</label>
                        <input type="file" name="file" class="form-control-file" onchange="previewFile()" id="file">
                    </div>
                </form>

                <!-- Vista previa de la imagen -->
                <div class="text-center">
                    <img id="preview" class="img-fluid border mt-3" height="200" style="display: block; max-width: 400px; margin: auto;">
                </div>

                <!-- Botón para generar el presupuesto en PDF -->
                <div class="text-center">
                    <form action="{{ url_for('generate_pdf') }}" method="post" class="mt-4">
                        <button type="submit" class="btn generate-btn btn-lg">
                            <i class="fas fa-file-pdf"></i> Generar Presupuesto en PDF
                        </button>
                    </form>
                </div>
            </div>
        </div>
    </div>

    <!-- Pie de página con fondo gris oscuro -->
    <footer class="footer text-white text-center py-2 mt-5">
        II-2024 - F.I.C.C.T.
    </footer>

    <!-- Script para vista previa de la imagen seleccionada -->
    <script>
        function previewFile() {
            const preview = document.getElementById('preview');
            const file = document.querySelector('input[type=file]').files[0];
            const reader = new FileReader();

            reader.onloadend = function () {
                preview.src = reader.result;
            }

            if (file) {
                reader.readAsDataURL(file);
            } else {
                preview.src = "";
            }
        }
    </script>

    <!-- Scripts de Bootstrap para interactividad -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
c="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

